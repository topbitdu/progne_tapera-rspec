require 'spec_helper'

describe ProgneTapera::Rspec do
  it 'has a version number' do
    expect(ProgneTapera::Rspec::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
